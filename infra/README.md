# Local Infrastructure

This directory contains local-only infrastructure configuration for security lessons.

- `postgres/`: database examples.
- `redis/`: rate-limit and session examples.
- `minio/`: local object storage and file upload examples.
- `nginx/`: reverse proxy and server configuration examples.

Do not connect this lab to public cloud services.

