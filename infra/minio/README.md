# Local MinIO Notes

MinIO is included only for local object-storage lessons. Use the fake credentials from `.env.example`.

Future file-upload stages should create deterministic buckets and objects during setup. Do not connect this lab to real cloud storage.

