# Reports

Generated BTS_sec reports go under `reports/generated/`.

Generated comparison files go under `reports/comparisons/`.

Optional golden report snapshots can go under `reports/snapshots/` when a task explicitly asks for committed fixtures.

Expected findings live under `expected/`.
